apiVersion: v1
kind: ConfigMap
metadata:
  name: ecommerce-config
  namespace: ecommerce

data:
  DB_HOST: postgres-service
  DB_PORT: "5432"
  DB_NAME: ecommerce_db
  DB_USER: ecommerce_user
  PORT: "5000"
